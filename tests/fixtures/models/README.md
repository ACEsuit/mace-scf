# Local Model Fixtures

Place local `.model` files used by model regression tests in this directory.
Model files are ignored by git.

Set `MACE_SCF_TEST_MODELS_DIR` to use a different local model fixture directory.
